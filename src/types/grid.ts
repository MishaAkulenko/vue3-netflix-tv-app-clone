export interface Grid {
  row: number;
  column: number;
  // rows?: number[]; // [1,2,3] - на майбутнє можна додати підтримку великих компонентів
  // columns?: number[]; // [1,2] - на майбутнє можна додати підтримку великих компонентів
}
